#!/bin/zsh
set -euo pipefail

echo "BRAIN_BENCHMARK_LANE=${BRAIN_BENCHMARK_LANE:-}"
echo "BRAIN_EMBEDDING_PROVIDER=${BRAIN_EMBEDDING_PROVIDER:-}"
echo "BRAIN_EMBEDDING_MODEL=${BRAIN_EMBEDDING_MODEL:-}"
echo "BRAIN_EXTERNAL_AI_EMBEDDING_MODEL=${BRAIN_EXTERNAL_AI_EMBEDDING_MODEL:-}"
echo "BRAIN_OPENROUTER_EMBEDDING_MODEL=${BRAIN_OPENROUTER_EMBEDDING_MODEL:-}"
echo "OPENROUTER_API_KEY=$( [[ -n ${OPENROUTER_API_KEY:-} ]] && echo set || echo unset )"
